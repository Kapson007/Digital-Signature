<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Generowanie kluczy</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins&display=swap');
        body{
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            height: 100vh;
            width: 100vw;
            background: linear-gradient(#141e30, #243b55);
            font-family: 'Poppins';
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            
        }
        .form{
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start;
            min-height: 60vh;
            max-width: 50vw;
            padding: 1rem;
            border: 2px solid rgba(0, 0, 0, 0.7);
            background: rgba(0, 0, 0, 0.5);
            box-sizing: border-box;
            box-shadow: 0 15px 25px rgba(0, 0, 0, 0.6);
            border-radius: 10px;
            color: aliceblue;
        }
        
        .box__submit{
            
            min-width: 50%;
            min-height: 12%;
            font-size: 1.2rem;
            font-weight: 600;
            font-family: 'Poppins', sans-serif;
            letter-spacing: .15rem;
            background-color: #33557b;
            color: #e6e6ed;
            border: 3px solid #243b55;
            border-radius: 5px;
            cursor: pointer;
            margin-top: 2rem;
        }
        
    </style>
</head>
<body>
    <form class="form" action="/verifySignature" method="post">
        <h2>Wygenerowano klucz dla twojej wiadomości</h2>
        <h3>Twój klucz to: </h3>
        <%=locals.key %> 
        <input class="box__submit" type="submit" value="Podpisz i wyślij">
    </form>
</body>
</html>